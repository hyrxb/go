package main

import "fmt"

func main(){
	//数组定义，指定的元素个数必须是常量

	var a [10]int
	var b [5]int

	fmt.Printf("len(a)=%d,len(b)=%d\n",len(a),len(b))

	//n :=10
	//var c [n]int //non-constant array bound n


}
